# 验证记录（2026-09-09）

- Python账本与模拟网络适配器测试：19项通过；覆盖独立work/verify次数、最后一次work后的verify、中断不重放、重复回执、证据哈希、缺验收项、独立任务越过阻塞依赖、时间/次数停止、并发锁、错误端点、禁用工具、超时与输出拒绝覆盖。
- `quick_validate.py`：技能结构通过。
- PowerShell AST：适配器语法通过；本机CLI帮助确认支持 `--tools`、`--bare` 和 `--strict-mcp-config`。
- 真实网络冒烟：指定 `grok-4.6`、使用本机已配置网关；只发送合成数字2与3，不发送源码或私人资料。work返回total=5；宿主独立计算后发起单独verify，实际返回通过。宿主读回两次输出及计算结果后才将账本置为completed。工具关闭；没有由Grok执行shell或修改文件。
- 两次真实调用均使用180秒超时和0.25美元CLI单次上限，无重试、无模型切换。仅记录配置请求模型，不能据此独立证明网关上游供应商身份或实际计费。
- 本机原始证据保存在 `D:\tmp\grok-general-smoke-20260909`，不随Git上传；网络结果经宿主审核，不直接充当任务回执。

边界：这证明本机的合成数据任务work→host check→verify→completed流程可运行。不是所有模型、网关、领域、权限和生产任务均已测试的保证。离线测试中的模拟响应不能冒充真实模型执行。

## 后续真实工具任务（同日）

最新26项测试通过。真实Claude用户配置下的CSV任务首轮空转；反馈后总额修正，但冻结宿主验收仍有3个边界失败，最后被hook停止。此后不追加同类重试，不将CLI success或模型自测当作全部验收通过。见 `../observations/grok-effective-simulation-20260909.json`。新增classify-claude-run.py区分真正验收与空结果/被hook停止。上面的纯算术冒烟只证明其有限范围，不覆盖此后复杂任务。

## 2026-09-10 实际文档任务

离线测试40项通过。实际Claude工具任务出现路径误判、产物通过但循环不结束、跨run加粗失败等问题；改用宿主已测试工具后，Grok完成3次Bash调用并正常结束，独立5项检查通过。角色与限制见 ../observations/grok-artifact-repair-20260910.json。这只证明有界工具辅助路径，不能保证任意任务稳定；没有改写或渲染真实简历。

后续错误恢复测试：真实Grok按安装守卫生成的诊断指令完成3个有效Bash步骤，4轮正常结束，独立5项检查通过。错误Write为合成夹具，未声称真实旧会话已恢复。见 ../observations/grok-docx-recovery-20260910.json；离线测试42项。
