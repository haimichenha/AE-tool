# 光照分类首轮检验：Glare（2026-09-09）

当前只读盘点25个Lighting AEX，选择定义路径较清楚的S_Glare作为首样本；未批量部署。PiPL中文列表名不能证明参数面板已汉化。同名DSL扫描基于410C分析核心，不代表所有正式入口的实际加载路径（正式Rays在独立rays09核心）。没有找到同名定义的条目仍需追踪别名/派生关系，不能据此判断不支持汉化。

## 实际完成

- Glare定位到36个参数、1个定义绝对指针引用；新增构建器按哈希锁定输入、重新解析定义及指针，不照搬Rays的定义偏移。
- 用户明确授权后，向当前配置Grok发送参数键、英文标题与简短含义；未发送DLL、注册码、账号或素材。
- Grok完成2次有界词表调用。第二次纠正了大部分用语，但把“由Alpha生成眩光”反译为“眩光生成Alpha”。宿主单独修正这一项并在候选词表记录来源；不声称模型独立全部完成。
- 候选映射：`maps/Glare-zh-CN.candidate.json`；核心SHA256：`7F077A162E7DF5A264A05D10BA5BB6D1AC3B9E40647E66EB0D0EF4D2FB82F223`。
- 构建仅插入title、修改一个定义指针及PE元数据；其余原字节保持不变。占位标题结构测试重复构建一致，缺键/多键/非法引号/超长标题4条拒绝路径均通过。
- 25个正式AEX和基线核心最后读回哈希均未改变。

## 严格边界

候选核心在 `D:\tmp\lighting-localization-20260909\translated-candidate`，**尚未生成并安装正式可用的测试AEX/runtime，没有进行AE显示或渲染验收**。`structural-probe`目录只含测试占位标题，不能安装。

此核心来自既有Light3D/Mocha基线，不包含正式Rays核心里的后续汉化；不能覆盖l3dao或rays09。下一步只为Glare创建隔离入口，然后检查参数显示、渲染、关键帧、保存重开、预设及Mocha调用。按钮、分组、下拉项和动态UI仍是另行验收项。

没有换号或重新认证账号。调用成功和译文质量不足均不能证明“账号被标记”。本轮未推送仓库，原始模型请求/响应仅留在本机任务目录。
